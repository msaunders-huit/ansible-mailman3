# ansible-mailman3
This Ansible role is meant to be used as a git submodule in a deployment playbook.
Override `defaults` using vars file or Tower task vars to customize configuration.

There have been significant changes since cloning from the [upstream](https://github.com/galaxyproject/ansible-mailman3) source repo, but rebasing _shouldn't_ be too bad if it ever gets updated.
